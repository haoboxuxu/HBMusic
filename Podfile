# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'HBMusic' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HBMusic
  # pod 'SDWebImage', '~> 5.0'
  pod 'HBWebImage', '0.1.1'
  pod 'HBGradientBlur'
  pod "Appirater"
end
